overlay: 1.2.0
info:
  title: Reusable action parameter items must include name in components
  version: 1.0.0
components:
  actions:
    setTitle:
      target: '$.info'
      parameters:
        - default: fallback
      update:
        title: Example
actions:
  - target: '$'