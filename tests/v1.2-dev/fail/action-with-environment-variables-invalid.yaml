overlay: 1.2.0
info:
  title: Reusable action environment variables must be an array
  version: 1.0.0
actions:
  - target: '$.info'
    environmentVariables:
      name: stageName
    update:
      description: Example
