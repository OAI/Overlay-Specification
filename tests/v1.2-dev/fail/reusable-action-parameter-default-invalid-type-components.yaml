overlay: 1.2.0
info:
  title: Reusable action parameter default values must be strings in components
  version: 1.0.0
components:
  actions:
    setTitle:
      target: '$.info'
      parameters:
        - name: titleValue
          default: 100
      update:
        title: Example
actions:
  - target: '$'
