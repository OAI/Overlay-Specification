overlay: 1.2.0
info:
  title: Reusable action environment variables must be an array in components
  version: 1.0.0
components:
  actions:
    setDescription:
      target: '$.info'
      environmentVariables:
        name: stageName
      update:
        description: Example
actions:
  - target: '$'