overlay: 1.2.0
info:
  title: Reusable action parameter names must be strings in components
  version: 1.0.0
components:
  actions:
    setTitle:
      parameters:
        - name: 100
      fields:
        target: '$.info'
        update:
          title: Example
actions:
  - target: '$'