overlay: 1.2.0
info:
  title: Reusable action parameter items must be objects
  version: 1.0.0
components:
  actions:
    setTitle:
      parameters:
        - invalid
      fields:
        target: '$.info'
        update:
          title: Example
actions:
  - target: '$'