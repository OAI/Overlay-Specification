overlay: 1.2.0
info:
  title: Non-reusable action objects can't have parameters
  version: 1.0.0
actions:
  - target: '$.info'
    parameters:
      - name: notAllowedHere
    update:
      title: Example
